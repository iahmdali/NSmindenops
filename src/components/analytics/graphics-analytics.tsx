import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function GraphicsAnalytics() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Graphics Department Analytics</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Analytics for the Graphics department will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
