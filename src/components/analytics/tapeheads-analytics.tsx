import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function TapeheadsAnalytics() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Tapeheads Department Analytics</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Analytics for the Tapeheads department will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
